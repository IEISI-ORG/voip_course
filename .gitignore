# --- Secrets: NEVER commit (see ~/.claude/rules/security.md) ---
.env
.env.*
!.env.example
secrets/
pki/
*.pem
*.key
*_secret*
*_token*
credentials.json
settings.json

# --- Lab runtime artifacts ---
lab/**/data/
lab/**/recordings/
lab/**/*.pcap
lab/**/*.log
*.sqlite
*.db

# --- Claude Code session state (not project content; history purge deferred) ---
.claude/

# --- Feedback files: ephemeral steering, processed then deleted, never committed ---
/feedback*
/FEEDBACK*
/feedback/

# --- Source papers dropped for ingestion: never committed (only distilled cites land in content) ---
/papers/

# --- OS / editor ---
.DS_Store
*.swp

# -- DNC --
SSCA-Elite-SIP-training.md


# --- MARP (E4) build artifacts ---
course/instructor/marp/node_modules/
course/instructor/marp/out/
course/instructor/marp/package-lock.json

# --- Stray saved web pages (copyrighted third-party; belong in papers/, not the repo) ---
/index.html
/*_files/
/understanding-*.html
