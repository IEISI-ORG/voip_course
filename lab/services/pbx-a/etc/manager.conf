;; VoIPSec pbx-a — AMI (Stage A2). DISABLED by default: AMI is a powerful remote-control
;; surface and a common breach vector (threat T11). Enabled with TLS + ACLs only when a
;; module needs it (M16 monitoring), never exposed beyond the mgmt plane.
[general]
enabled = no
webenabled = no
