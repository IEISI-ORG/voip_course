;; SOVOC pbx-a — HTTP/ARI (Stage A2). DISABLED by default. The built-in HTTP server backs
;; ARI and other interfaces; it stays off to minimise attack surface (threat T11) and is
;; turned on with TLS + auth only where a lab explicitly needs ARI.
[general]
enabled = no
